/**Name : URL Module
 * Description: The ‘url’ module provides utilities for URL resolution and parsing. The getters and setters implement the properties of URL objects on the class prototype, and the URL class is available on the global object
 * Example:const searchpar = new URLSearchParams("keya=vala&keyb=valb");
 
for (let pair of searchpar.entries()) {
    console.log(pair[0] + ', ' + pair[1]);
}
  */