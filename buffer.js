/**Name : Buffer Module
 * Description: Buffers are designed to handle binary raw data.
 * Example: const cbuf = Buffer.alloc(256);
bufferlen = cbuf.write("Let's do this 
    with GeeksForGeeks");
console.log("No. of Octets in which 
    string is written : "+  bufferlen);

  */