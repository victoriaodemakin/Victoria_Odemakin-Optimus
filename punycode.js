/**Name : Punycode Module
 * Description: Punycode is a special encoding syntax that is specifically used to convert Unicode characters (UTF-8) to ASCII, which is nothing but the restricted string character set.
 * Example:
const punycode = require('punycode');
 console.log(punycode.decode('manama-pta'));
console.log(punycode.decode('--dqo34k'));

  */